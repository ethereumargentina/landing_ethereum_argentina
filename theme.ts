import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            light: '#B4F4FE',
            main: '#152640',
            dark: '#0B1628'
        },
        secondary: {
            main: '#74ACDF',
        }, 
    },
});

export default theme;